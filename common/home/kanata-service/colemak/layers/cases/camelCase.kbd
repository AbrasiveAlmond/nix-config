(deflayer camel
  tab  q    @hw  @hf  p     b    j    l    @hu  y    ;
  @cps @sa  @Ar  @Cs  @St   g    m    @Sn  @Ce  @Ai  @so   @home
  @smb @Mz  x    c    d     v    k    h    ,  .  @M/   @smb
  lctl lmet lalt        @spc-camel         _   _    _
)
(defalias
    spc-camel (
        tap-hold-release 150 150
        (tap-dance 500 (
            (one-shot 800 lsft)
            (multi spc @home)
        ))
        (layer-while-held maths)
    )
)
